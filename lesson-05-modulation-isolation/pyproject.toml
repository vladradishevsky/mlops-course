[project]
name = "lesson-05-modulation-isolation"
version = "0.1.0"
description = "Demo project for modulation and isolation lesson"
readme = "README.md"
requires-python = "==3.11.*"
dependencies = [
    "pandas==2.3.3",
    "scikit-learn==1.8.0",
]

[dependency-groups]
dev = [
    "pytest==9.0.2",
    "pytest-cov>=7.1.0",
]
